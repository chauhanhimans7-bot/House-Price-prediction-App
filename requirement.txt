streamlit==1.54.0          # or just streamlit (cloud will use recent)
numpy
xgboost>=2.0.0             # or exact: xgboost==2.1.1 (check your local version with pip show xgboost)
pandas                     # if you use it anywhere
scikit-learn               # often needed for preprocessing in Boston models
